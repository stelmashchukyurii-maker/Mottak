# BaMavaremottak — індекс протоколів проєкту

**Призначення:** постійна точка входу для нових розмов ChatGPT, розробників і тестувальників.  
**Репозиторій:** `stelmashchukyurii-maker/Mottak`  
**Основна гілка:** `main`  
**Оновлено:** 08.08.2026 20:30 Europe/Oslo

---

## 1. Як користуватися цим індексом

Перед продовженням будь-якої розробки потрібно:

1. визначити напрям роботи: INN, UT, головна сторінка, продукти або інфраструктура;
2. прочитати відповідний протокол нижче;
3. після протоколу обов’язково відкрити актуальні файли в `main`;
4. вважати протокол джерелом погоджених рішень і результатів тестів;
5. вважати поточний код `main` джерелом фактичної реалізації;
6. при суперечності між старими протоколами використовувати найновіший датований документ;
7. після значної зміни створити новий датований протокол або оновити чинний документ із новою датою й журналом змін.

---

## 2. Поточні основні протоколи

### НАЙНОВІШИЙ HANDOFF — старт нової розмови

**Починати з цього документа:**

- `docs/HANDOFF_2026-08-08_2030.md`

**Код для короткого старту:**

```text
Продовжуй BAMA-HANDOFF-0808.
```

Handoff фіксує актуальний стан на 08.08.2026 20:30:

- WORKING / PRODUCTION сторінки;
- GREEN / паралельний клон;
- TEST / ізольований контур;
- центральний `products.js` і 7 активних продуктів;
- нові Forlengere;
- актуальні TEST UT Kontor / UT Lager файли;
- останній root-fix LIVE TOTALS;
- TEST Supabase таблиці, RPC і migration;
- останню перевірену TEST-рампу;
- точний acceptance test для наступного кроку;
- правило TEST → GREEN → підтвердження → production.

### UT-TEST-CHAIN — ізольований тестовий ланцюг

**Архітектурний протокол:**

- `docs/UT_TEST_CHAIN_PROTOCOL_2026-08-08.md`

**Статус реалізації:**

- `docs/UT_TEST_CHAIN_STATUS_2026-08-08.md`

Код протоколу:

```text
UT-TEST-CHAIN
```

На 08.08.2026 TEST-контур уже реалізований значно далі початкового плану: існують окремі TEST-таблиці Supabase, TEST UT Kontor, TEST UT Lager, TEST Camera/Foto, нові Forlengere і окремі TEST-RPC. Актуальний стан після 20:30 брати насамперед із handoff вище та фактичного коду `main`.

### INN — приймання товару

**Основний документ:**

- `docs/INN_PROTOCOL_2026-08-03.md`

Містить:

- головну архітектуру INN;
- правило одного спільного запису `mottak_scans` для Nordic ID і телефона;
- формати EPC `24 → 12 / 6 / 6`;
- статуси `pending`, `verified`, `invalid`;
- актуальні телефонні та Nordic ID файли;
- Camera Cloud із серверним Gemini та резервом OpenAI;
- результати фактичних тестів Gemini й OpenAI;
- відомі дефекти та ризики;
- правило, що Nordic ID є основним шляхом, а AI — резервом.

Перед зміною INN потрібно відкрити актуальні INN-файли `main`, зокрема чинну Camera Cloud сторінку. Старі назви у протоколі можуть бути історичними; фактичний код `main` має вищий пріоритет для поточного стану.

Ключове чинне рішення INN:

```text
ОСНОВНИЙ ШЛЯХ:
Nordic ID / фірмовий сканер

РЕЗЕРВ І ФОТОФІКСАЦІЯ:
телефон + Gemini

ДРУГИЙ РУЧНИЙ РЕЗЕРВ:
телефон + OpenAI
```

### UT — production / історична основа

**Основний production-протокол:**

- `docs/UT_PROTOCOL_2026-08-04.md`

**Попередній історичний документ:**

- `docs/UT_PROTOCOL_2026-08-03.md`

Вони описують production-архітектуру UT та історію переходу до TESTMODE. Для поточної TEST-розробки 08.08.2026 використовувати новіші `UT_TEST_CHAIN*` і `HANDOFF_2026-08-08_2030.md`.

Головне production-правило залишається:

```text
Production не змінювати під час TEST-налагодження.
Не переносити TEST-архітектуру на робочі сторінки без окремої команди користувача.
```

---

## 3. Правило назв нових протоколів

Рекомендований формат:

```text
docs/<DIRECTION>_PROTOCOL_YYYY-MM-DD.md
```

Для handoff дозволено:

```text
docs/HANDOFF_YYYY-MM-DD_HHMM.md
```

Кожен новий протокол повинен містити:

- дату й час;
- напрям;
- статус документа;
- перелік актуальних файлів;
- що реалізовано;
- що фактично протестовано;
- точні результати тестів;
- відомі дефекти;
- ризики;
- найближчий пріоритет;
- список рішень, які не можна змінювати без погодження;
- текст для старту нової розмови;
- посилання на протокол, який він замінює або доповнює.

---

## 4. Дисципліна оновлення

Протокол потрібно оновити після:

- зміни структури Supabase;
- зміни бізнес-логіки товарів;
- нового стабільного релізу;
- успішного повного тесту;
- виявлення критичного дефекту;
- зміни статусів або життєвого циклу;
- додавання авторизації;
- переходу з TEST → GREEN або GREEN → production.

Не потрібно створювати новий протокол для дрібної зміни кольору або тексту кнопки, якщо вона не змінює процес.

---

## 5. Рекомендований порядок перевірки новою розмовою

Нова розмова не повинна одразу змінювати код.

Для поточного стану 08.08.2026 вона має:

1. прочитати `docs/HANDOFF_2026-08-08_2030.md`;
2. за потреби прочитати `docs/UT_TEST_CHAIN_PROTOCOL_2026-08-08.md` і `docs/UT_TEST_CHAIN_STATUS_2026-08-08.md`;
3. отримати актуальні файли з GitHub `main`;
4. при роботі з Supabase прочитати Supabase skill і перевірити актуальну схему/migrations;
5. коротко підсумувати поточний стан;
6. лише після цього вносити зміни.

Не покладатися тільки на старі номери версій у пам’яті — перевіряти фактичний код.

---

## 6. Стартовий текст для нової розмови

### Найкоротший

```text
Продовжуй BAMA-HANDOFF-0808.
```

### Повний

```text
Продовжуємо проєкт BaMavaremottak / Mottak.
Репозиторій: https://github.com/stelmashchukyurii-maker/Mottak
Гілка: main.

Спочатку прочитай docs/HANDOFF_2026-08-08_2030.md.
Потім, якщо потрібно, прочитай PROTOCOLS.md та UT-TEST-CHAIN.
Після цього відкрий актуальні файли коду з main.

Production не змінюй без мого окремого підтвердження.
Не виправляй конкретні TEST-рампи вручну як рішення багу.
Якщо є проблема — простеж ланцюг UI → RPC → TEST-база → повторне читання → render.

Спочатку коротко скажи:
1) який зараз TEST-стан;
2) що вже виправлено;
3) який acceptance test треба виконати наступним.
```

---

## 7. Поточний найближчий пріоритет проєкту

### UT TEST — пріоритет на 08.08.2026 20:30

Після root-fix `LIVE TOTALS` не робити нових змін навмання. Спочатку пройти нове замовлення:

```text
TEST UT Kontor
→ створити НОВУ TEST-рампу
→ замовити Bunner/H30/H60 + Forlengere korte/lange
→ TEST UT Lager
→ підтвердити конкретні товари
→ ввести різні фактичні Hyller + Forlengere для korte/lange
→ перевести На рампу
→ повернутися в TEST UT Kontor
→ перевірити автоматично, без ручної правки:
   Bunner total
   Hyller total
   Forlengere korte actual
   Forlengere lange actual
   CC Post
```

Якщо це працює:

```text
TEST PASS
→ переносимо погоджені зміни в GREEN
→ перевіряємо GREEN
→ користувач окремо підтверджує
→ production
```

### INN

Чинна production-лінія Camera Cloud та Nordic ID розвивається окремо. Не змішувати INN production із нинішнім UT TEST acceptance test.

---

## 8. Принцип збереження історії

Протоколи не потрібно видаляти після створення нової версії.

Старі документи залишаються історією. Новіший handoff/протокол має вищий пріоритет для поточного стану, але перед зміною коду все одно потрібно відкрити фактичні файли `main`.

Порядок джерел істини:

```text
погоджені бізнес-рішення користувача
→ найновіший handoff / протокол
→ актуальний код main для фактичної реалізації
→ фактична схема Supabase для даних
```
