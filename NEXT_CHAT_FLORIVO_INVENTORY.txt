FLORIVO-INVENTORY-HANDOFF-2026-08-23-SERVER-SYNC

READ FIRST:
1. PROJECT_CANONICAL_RULES.md
2. BAMAVAREMOTTAK_TEST_LIVE_PROTOCOL.md
3. PROTOCOLS.md
4. FLORIVO_INVENTORY_CONCEPT_PROTOCOL.md
5. FLORIVO_NUMBER_PROTOCOL.md
6. NORDIC_ID_RFID_PROTOCOL.md
7. FLORIVO_ANDROID_SCANNER_CONCEPT_PROTOCOL.md

CURRENT PAGES:
- florivo-inventory-sync.html — shared server-synced Inventory scanner/client
- florivo-inventeringer.html — PC/web visual + table dashboard, reads same server session
- florivo-inventory-test-v011-stable.html — frozen rollback copy
- florivo-inventory-v012-dev.html — prior localStorage V0.12 prototype

CURRENT SERVER MILESTONE:
- Supabase project: BaMavaremottak
- New append-only table: public.florivo_inventory_events
- Migration: create_florivo_inventory_event_sync
- RLS enabled
- anon/authenticated can SELECT inventory events and INSERT only source='florivo-inventory', mode test/live, event_type='snapshot'
- No update/delete policy; history is append-only.
- Current development mode remains mode='test' per canonical safety rule until explicit physical PASS.
- Inventory server sync does NOT mutate WORK stock, orders, production counters or Nordic WORK flows.

SYNC MODEL:
- scanner/phone writes a full inventory-session snapshot as an append-only event after each meaningful change;
- PC/web polls latest snapshot approximately every 2.5 seconds;
- all devices therefore see the same latest inventory session;
- existing V0.12 localStorage session is uploaded on first use if server has no snapshot, preserving current manual test data;
- localStorage remains a local fallback/cache.

CURRENT ZONES:
- Plukk
- Varm
- Kald
- Demontering
- Produksjon
- CC
Ramper 28–34 are visual orientation references and match UT Kontor ramp numbering.

CURRENT INVENTORY UX:
- NO / UK
- zone selection visual/table
- current zone visible while counting
- product remembered
- RFID / Nordic wedge path
- manual MAN-xxx path
- simulated RFID for non-Nordic testing
- zone journal and counters
- zone FULLFØR
- zone RECOUNT with revision
- manual batch for 1 Bunner + Hyller: e.g. 50 Bunner × 3 Hyller = 150 Hyller in one manual observation
- RFID individual identity remains individual; batch manual must never fabricate RFID/EPC.

IMPORTANT RULES:
- Inventering records physical reality and compares later; it does not silently correct WORK stock.
- Duplicate RFID in one inventory session must be blocked and point to prior zone/revision.
- scanner_code = full real EPC.
- lower_number = last 6 of real EPC only.
- florivo_number is a separate permanent identity.
- no fake RFID/lower.
- if printed physical tag number is distinct, create a separate field after verification.

NEXT:
1. User test: enter on phone/scanner and verify PC sees same records.
2. Validate full Nordic physical RFID flow against florivo-inventory-sync.html before PHYSICAL PASS.
3. Improve server architecture from snapshot-events to normalized inventory sessions/observations if concurrency/multi-scanner writing is needed.
4. Add historical inventory list on PC.
5. Build SERVER EXPECTED ↔ FAKTISK and AVVIK.
6. Add separate audited correction flow only after review.

IMPORTANT:
AVSLUTT INVENTERING never silently corrects LIVE/WORK stock.
